# Mnemo VS Code Extension (MVP)

This extension provides basic Mnemo workspace commands:

- Detect Mnemo install
- Initialize workspace (`mnemo init`)
- Show status (`mnemo doctor`)
- Refresh index (`mnemo map`)

## Development

```bash
npm install
npm run compile
```
